function randomLoop(array) {
    for (let i=0; i< array.length; i++) {
        const result= array[i];
        console.log(result);
    }
}

const primeArray = ['apple' , 'your mom' , 'jiho' , 'eddie' , 'google' , 'mom'];
randomLoop(primeArray);